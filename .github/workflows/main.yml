name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  call-reusable:

    uses: manojkolagana25/github-reusable-workflow-demo/.github/workflows/reusable-build.yml@main

    with:
      app_name: "sample-app"
